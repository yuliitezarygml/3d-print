"use client";
import { useState } from "react";import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { Plus } from "lucide-react";import { api,money } from "@/lib/api";import { ErrorNote,Modal,PageHeader } from "@/components/ui";
type Customer={id:string;name:string;company?:string;phone?:string;email?:string;orderCount:number;totalAmount:number;modelCount:number};
export default function Customers(){const [open,setOpen]=useState(false);const qc=useQueryClient();const q=useQuery({queryKey:["customers"],queryFn:()=>api<Customer[]>("/api/customers")});const add=useMutation({mutationFn:(x:Record<string,unknown>)=>api("/api/customers",{method:"POST",body:JSON.stringify(x)}),onSuccess:()=>{qc.invalidateQueries({queryKey:["customers"]});setOpen(false)}});return <><PageHeader eyebrow="CRM" title="Клиенты" description="Контакты, личная библиотека моделей, история заказов и выручка." actions={<button className="button primary" onClick={()=>setOpen(true)}><Plus size={17}/>Новый клиент</button>}/><div className="data-card table-wrap"><table className="table"><thead><tr><th>Клиент</th><th>Контакты</th><th>Моделей</th><th>Заказов</th><th>Выручка</th></tr></thead><tbody>{q.data?.map(c=><tr key={c.id}><td><strong>{c.name}</strong><small>{c.company||"Частный клиент"}</small></td><td><strong>{c.phone||"—"}</strong><small>{c.email||"—"}</small></td><td><strong>{c.modelCount}</strong><small>личная библиотека</small></td><td>{c.orderCount}</td><td><strong>{money(c.totalAmount)}</strong></td></tr>)}</tbody></table></div>{open&&<Modal title="Новый клиент" onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);add.mutate({name:f.get("name"),company:f.get("company")||null,phone:f.get("phone")||null,email:f.get("email")||null})}}><label className="field full">Имя<input name="name" required/></label><label className="field">Компания<input name="company"/></label><label className="field">Телефон<input name="phone"/></label><label className="field full">Email<input name="email" type="email"/></label>{add.error&&<div className="field full"><ErrorNote error={add.error}/></div>}<div className="form-actions"><button type="button" className="button" onClick={()=>setOpen(false)}>Отмена</button><button className="button primary" disabled={add.isPending}>Добавить</button></div></form></Modal>}</>}
